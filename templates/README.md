# Starter cartridges

Each directory is an independent, deliberately small PXCL/1 project. Copy a directory, change its
`id` and `title`, then run `px240c check`, `run`, or `pack`. These are direct console programs and
do not add an engine abstraction layer.
